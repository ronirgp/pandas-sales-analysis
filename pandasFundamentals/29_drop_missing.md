# Drop missing values

`dropna()` removes rows that contain missing values.

Example:

```python
df.dropna()


**Exercise:** Use `dropna()` to remove rows with missing values.

# My solution
df.dropna()