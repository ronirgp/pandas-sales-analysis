# Sum values

`sum()` adds all the values in a column together.

Example:

```python
df["quantity"].sum()

Find the total of all the price values.

My solution:

# write your solution here

Your turn: **find the total price.**

df["price"].sum()