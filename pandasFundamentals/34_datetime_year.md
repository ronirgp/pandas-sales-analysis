# Get the year from a date

`.dt.year` gets the year from a datetime column.

Example:

```python
df["date"].dt.year


**Exercise:** Use `.dt.year` with the `date` column.

# My solution

df["date"].dt.year