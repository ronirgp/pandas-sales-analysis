# Standard deviation

`std()` measures how spread out the values are.

Example:

```python
df["price"].std()

Find the standard deviation of quantity.

# write your solution here

Your turn: **find the standard deviation of `quantity`.**

My solution:
df["quantity"].std()