# rename

`rename()` changes column names.

Example:

```python
df.rename(columns={"price": "unit_price"})


### Your task

Rename:

**`customer` → `customer_name`**

This time, use the `rename()` pattern from the example. Send me your code.

# My solution
df.rename(columns={"costomer": "costomer_name"})
