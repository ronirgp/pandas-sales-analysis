# nunique

`nunique()` counts how many different values are in a column.

Example:

```python
df["product"].nunique()

### Your exercise

> **Count how many different customers are in the dataset.**

solution here

df["customer"].nunique()