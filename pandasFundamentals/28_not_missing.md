# Not missing values

`notna()` checks which values are NOT missing.

Example:

```python
df["price"].notna()


**Exercise:**  
Use `.notna()` on the `customer` column.

Send me your solution when you're done.

# My solution
df["customer"].notna()