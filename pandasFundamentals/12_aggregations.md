# Aggregations

Aggregation functions calculate values from a column.

Examples:

```python
df["quantity"].sum()
df["price"].mean()
df["quantity"].max()

**Find the total quantity sold.**

Write the code yourself and send it to me.
# solution here
df["quantity"].sum()

