# value_counts

`value_counts()` counts how many times each value appears.

Example:

```python
df["region"].value_counts()


###  exercise

> **Count how many orders there are for each product.**

Send me your code.

My solution 
dfdf["product"].value_counts()

