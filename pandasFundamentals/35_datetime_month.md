# Get the month from a date

`.dt.month` gets the month number from a datetime column.

Example:

```python
df["date"].dt.month


**Exercise:** Get the month from `date` using `.dt.month`.

# My solution.

df["date"].dt.month