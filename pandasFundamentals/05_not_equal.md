# Not Equal

Use `!=` when we want everything that is different from a value.

Example:

```python
df[df["region"] != "North"]



### 🎯 Your exercise

> **Show me all orders where `category` is NOT `"Electronics"`.**

Build the code yourself and send it to me.

Once I check it, we'll document it → commit → **next exercise**.


df[df["category"] != "Electronics"]