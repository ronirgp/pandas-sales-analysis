# Remove duplicates

`drop_duplicates()` removes duplicate rows.

Example:

```python
df.drop_duplicates()
```

This keeps the first occurrence and removes duplicate rows.

## Exercise 1

Remove duplicate rows from the DataFrame.

My solution:

```python
# write your solution here
```
df.drop_duplicates()