# loc

Use `loc` when we want to select specific rows and columns.

Example:

```python
df.loc[0, "customer"]


### 🎯 Your exercise

> **Show the `customer` and `region` for row 0.**

Think:

- row 0 → `0`
- columns → `customer`, `region`
- selecting by label → `.loc`

Build the code yourself and send it to me.

**Don't commit yet.** I'll check your answer first, then we'll document and commit it.

# write solution here
