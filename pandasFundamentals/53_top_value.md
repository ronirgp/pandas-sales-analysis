# Find the top value

Sometimes we don't need the whole sorted result. We only want the first result.

Example:

```python
df["quantity"].max()

Find the highest price in the DataFrame.

# My solution:

**Exercise:** Find the highest value in the `price` column.

df["price"].max()