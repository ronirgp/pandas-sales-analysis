# drop

`drop()` removes a column.

Example:

```python
df.drop(columns=["date"])


### Your task

> Remove the **`order_id`** column using `drop()`.

Use the example pattern and send me your code.

# My solution
df.drop(columns=["order_id"])