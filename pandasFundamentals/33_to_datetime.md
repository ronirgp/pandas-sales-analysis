# Convert to datetime

`pd.to_datetime()` converts a column into a datetime type.

Example:

```python
df["date"] = pd.to_datetime(df["date"])
```

This changes the `date` column from text to datetime.

## Exercise 1

Convert the `date` column to datetime.

My solution:

```python
# write your solution here
```
df["date"] = pd.to_datetime(df["date:)])