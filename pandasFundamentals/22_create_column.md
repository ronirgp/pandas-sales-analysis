# Create a new column

We can create a new column from existing columns.

Example:

```python
df["total"] = df["quantity"] * df["price"]



### Your task

> **Create a `total` column = `quantity × price`.**

Write the code yourself and send it to me.

# My solution

df["total"] = df["quantity"] * df["price"]
