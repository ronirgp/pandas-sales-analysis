# Merge DataFrames

`merge()` combines two DataFrames using a common column.

Example:

```python
pd.merge(df1, df2, on="customer")

# My solution


**Exercise:** Merge `df` and `df2` using `customer`.

pd.merge(df, df2, on="customer")