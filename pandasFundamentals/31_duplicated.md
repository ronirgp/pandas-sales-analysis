# Check duplicates

`duplicated()` checks for duplicate rows.

Example:

```python
df.duplicated()
```

This returns `True` for rows that are duplicates of an earlier row.

## Exercise 1

Check the DataFrame for duplicate rows.

My solution:

```python
# write your solution here
```
# My solution

df.duplicated()