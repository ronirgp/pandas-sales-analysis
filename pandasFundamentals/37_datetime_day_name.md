# Get the day name from a date

`.dt.day_name()` gets the name of the weekday from a datetime column.

Example:

```python
df["date"].dt.day_name()

# My solution

**Exercise:** Get the weekday name from `date` using `.dt.day_name()`.
df["date"].dt.day_name()