# Median value

`median()` finds the middle value when the numbers are ordered.

Example:

```python
df["quantity"].median()

Find the median price in the DataFrame.


# write your solution here

Your turn: **find the median price.**

My solution: 
df["price"].median()