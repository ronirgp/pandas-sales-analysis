# Concatenate DataFrames

`pd.concat()` combines DataFrames by stacking them together.

Example:

```python
pd.concat([df1, df2])


**Exercise:** Use `pd.concat()` to combine `df` and `df2`.

# My solution
pd.concat([df, df2])