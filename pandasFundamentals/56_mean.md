# Average value

`mean()` calculates the average of the values in a column.

Example:

```python
df["quantity"].mean()

Find the average price in the DataFrame.

My solution:

# write your solution here

Your turn: **find the average price.**

df["price"].mean()