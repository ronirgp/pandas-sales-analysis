# Missing values

`isna()` checks for missing values.

Example:

```python
df["price"].isna()

### Your task

Use `isna()` to check for missing values in **`customer`**.

Send me your code.

# My solution
df["customer"].isna()