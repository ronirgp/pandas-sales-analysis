# Missing values count

`isna().sum()` counts how many missing values are in a column.

Example:

```python
df["price"].isna().sum()


### Your task

> **Count the missing values in `customer`.**

Write the code and send it to me.

# My solution

df["customer"].isna().sum()